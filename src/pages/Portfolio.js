import React from 'react';
import Navigation from '../elements/Navigation';
import ProjectList from '../elements/Portfolio/ProjectList';

const Portfolio = () => {
    return (
        <div className="Portfolio">
            <Navigation/>
            <ProjectList/>
        </div>
    );
};

export default Portfolio;